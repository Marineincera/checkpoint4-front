import { CategoryPerformance } from './category-performance';

describe('CategoryPerformance', () => {
  it('should create an instance', () => {
    expect(new CategoryPerformance()).toBeTruthy();
  });
});
